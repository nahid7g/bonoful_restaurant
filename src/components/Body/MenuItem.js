import React from "react";

const MenuItem = (props) => {
    console.log(props);
    return (
        <div>
            <h1>Hello from menuitem</h1>
        </div>
    );
};

export default MenuItem;
